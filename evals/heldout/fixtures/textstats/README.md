# textstats

Small text-statistics utilities used by the reporting pipeline.

## Usage

```python
from textstats import word_count, unique_words, most_common
```

See `docs/` for the format specifications and `helpers/` for I/O adapters.

{
  "id": "leapyear",
  "category": "simple logic bug (boundary)",
  "fixture": "evals/heldout/fixtures/leapyear",
  "issue": "is_leap_year(2000) returns False, but the year 2000 was a leap year. Years divisible by 400 are leap years.",
  "ground_truth_files": [
    "calendar_utils.py"
  ],
  "reference_fix": {
    "path": "calendar_utils.py",
    "old": "    return year % 4 == 0 and year % 100 != 0",
    "new": "    return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)"
  },
  "max_steps": 30
}

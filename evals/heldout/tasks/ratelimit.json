{
  "id": "ratelimit",
  "category": "configuration/default bug",
  "fixture": "evals/heldout/fixtures/ratelimit",
  "issue": "Passing overrides to build_limits permanently changes the defaults: later calls with no overrides return the previous call's values.",
  "ground_truth_files": [
    "ratelimit.py"
  ],
  "reference_fix": {
    "path": "ratelimit.py",
    "old": "    limits = DEFAULTS\n",
    "new": "    limits = dict(DEFAULTS)\n"
  },
  "max_steps": 30
}
